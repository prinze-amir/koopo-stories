# Koopo Stories QA Report

## Date
March 1, 2026

## Scope Requested
Focused E2E QA for:
- Story creation flow with non-blocking/background upload UX.
- Story-to-activity sharing flow and media quality.

## Environment Check
- `docker ps` failed: Docker daemon/socket unavailable.
- `curl http://127.0.0.1:8085` failed: local WordPress endpoint not reachable.

Because the runtime stack is down, browser/API E2E execution could not be completed in this run.

## Completed QA (Non-runtime)
- Full PHP syntax scan of plugin files: pass.
- Full JS syntax scan of `assets/*.js`: pass.
- Targeted flow/code-path validation:
  - Background posting UX + sticky upload notice: implemented.
  - Activity preview media size defaults to `large` and is filterable.
  - Legacy activity card media snapshot path recomputes attachment-derived media.
  - Viewers endpoint timestamp mapping corrected (`last_viewed_at`).
  - Feed unseen calculation optimized to use one batched seen-map query.

## Commands Executed
- `find includes -name '*.php' -print0 | xargs -0 -n1 php -l`
- `for f in assets/*.js; do node --check "$f"; done`
- `php -l includes/stories/class-stories-share.php`
- `php -l includes/stories/rest/class-stories-rest-feed.php`
- `php -l includes/stories/rest/class-stories-rest-story.php`

## Pending Live E2E Steps (when stack is up)
1. Open site and log in.
2. Create new story from composer and confirm modal closes immediately while sticky notice tracks progress.
3. Verify story appears in tray after background completion.
4. Create story with “Also share to activity” enabled.
5. Validate activity card image/video quality against original source resolution.
6. Validate share from BuddyBoss activity modal and feed entries.
